# Pinned to the versions this backend is tested against, so the container runs
# what the test suite ran. pandas, PyMuPDF and Pillow are not used by the
# backend directly - they come in through extraction/extractor, which reads
# PDFs, images and spreadsheets.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.32
pydantic==2.13.4
python-dotenv==1.2.1
supabase==2.31.0
httpx==0.28.1

# shared with the existing extraction / deck pipelines
openpyxl==3.1.5
pandas==3.0.5
PyMuPDF==1.28.2
python-pptx==1.0.2
Pillow==12.3.0
requests==2.34.2

# Authentication (migration 0006). PyJWT verifies the Supabase access token
# against the project key set; httpx is the fallback path via the Auth API.
PyJWT==2.13.0
cryptography==49.0.0
